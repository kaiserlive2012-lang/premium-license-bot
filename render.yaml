services:
  - type: web
    name: premium-exe-license-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PORT
        value: 10000
      - key: DISCORD_TOKEN
        sync: false
